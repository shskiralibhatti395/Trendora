{
  "name": "trendora",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix Back-end\" \"npm run dev --prefix Front-end\"",
    "dev:server": "npm run dev --prefix Back-end",
    "dev:client": "npm run dev --prefix Front-end",
    "seed": "npm run seed --prefix Back-end",
    "install:all": "npm install && npm install --prefix Back-end && npm install --prefix Front-end"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
